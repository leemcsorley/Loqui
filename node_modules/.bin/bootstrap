#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../bootstrap/bin/bootstrap" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../bootstrap/bin/bootstrap" "$@"
  ret=$?
fi
exit $ret
